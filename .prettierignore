# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/

# Written by the Paraglide plugin on every build. The project's own nested
# .gitignore keeps them out of git, but Prettier only reads the root one — so
# without this a fresh CI checkout formats differently than a local tree that
# has already built once.
/project.inlang/*
!/project.inlang/settings.json
/src/lib/paraglide/
